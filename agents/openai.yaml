interface:
  display_name: "Codex OpenCode Worker Workflow"
  short_description: "Explicit personal workflow for Codex orchestration, OpenCode execution, and Codex acceptance on any Git project."
  default_prompt: "Use $codex-opencode-deepseek-workflow to inspect this Git project, create an AI development task order, run OpenCode with the configured worker model, review and verify the git diff, and report the final acceptance result."

policy:
  allow_implicit_invocation: false
